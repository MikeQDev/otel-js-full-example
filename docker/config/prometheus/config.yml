global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  # - job_name: "localhost" # if want to pull directly from app, without otel collector, use host machine IP (eg:192.168.4.170)
  #  scrape_interval: 5s
  #  static_configs:
  #    - targets: ["192.168.4.170:9464"]
  - job_name: 'demo'
    scrape_interval: 5s
    static_configs:
      - targets: ['otel-collector:8889'] # scrapes metrics from OTel collectors /metrics endpoint
